import 'package:flutter/material.dart';

class WordMatchWidget extends StatelessWidget {
  const WordMatchWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Word Match Game - Find Synonyms and Antonyms!'),
    );
  }
}